package adaboost;

import java.util.ArrayList;

public class DTCTree {
	
	public double calculateEntropy(ArrayList<InstanceTriplet> instances, int attribute) {
		double entropy = 0.0;
		int totalInstances = instances.size();
		
		if(instances.size() == 0)
			return 0.0;
		
		
		
		
		return entropy;
	}

}
